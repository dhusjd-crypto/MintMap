# Quiet, Working, and Weekend Hours

Quiet hours are explicit local-time policies and support cross-midnight ranges
such as `22:30` to `07:30`. Working hours are separate and may restrict only
selected source types. Weekend behavior is configurable. A critical level may
be allowed outside normal hours without making every notification critical.
