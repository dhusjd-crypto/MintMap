# Finance Provenance

Statements and import batches retain source document identifiers. Imported transactions retain batch, external ID and reference metadata. This supports duplicate detection and audit without copying raw document/OCR text into Finance domain events.
