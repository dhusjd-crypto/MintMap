# Quick Capture

`/capture` provides one-line capture, `Enter` parsing, `Ctrl/Cmd+K` focus, explicit clipboard capture and file references. A proposal can remain in the inbox or be confirmed into the selected first Mind Map node. Capture does not add tasks to today's plan automatically.
