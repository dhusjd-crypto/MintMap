# Finance Trigger Context

The evaluator accepts explicit FinanceBook-scoped obligations, payments, statements, schedules, a clock value, timezone and optional notification-history summary. It does not read IndexedDB, React state, Notification persistence or network services. The application coordinator is the only repository bridge.
