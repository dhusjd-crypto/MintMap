# Finance Import Trust

Structured bank files with FITID or bank reference can suggest `CLEARED` transaction state after user import confirmation. They are never automatically `RECONCILED`. User-mapped CSV and QIF have weaker identity evidence. OCR/screenshots remain proposal/review evidence and never reconcile ledger records by themselves.
