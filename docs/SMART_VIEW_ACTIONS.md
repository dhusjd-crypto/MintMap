# Smart View Actions

Smart View START, DONE and SNOOZE invoke canonical Execution domain transitions
through `smartViewsApplication.commands`. The canonical extension is saved first
and the compatible legacy Todo patch is written through the existing repository.
This keeps Smart Views, Focus and legacy task surfaces on the same task identity.

Views open the existing canonical source routes; they do not provide shadow task
or Finance detail records.
