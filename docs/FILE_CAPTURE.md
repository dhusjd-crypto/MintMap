# File Capture

Images, screenshots, PDFs and common text documents enter Capture as references with name, MIME type, size and creation time. Files are validated for non-empty content, supported type and a 25 MB default limit. Binary content is not copied into Task entities.
