# Finance Application

The Finance application is the only UI path to create manual books, accounts, transactions, transfers, statements, obligations and payments. React calls application commands; ledger balances and outstanding amounts are computed in Finance domain/application code.

Personal and Business books are selected explicitly and queried separately. No UI view aggregates books or currencies.
