# Turkish Capture Parser V1

The pure parser uses explicit local context and `TURKISH_CAPTURE_PARSER_V1`. It understands Turkish relative days, weekdays, dayparts, exact times, durations, `#Project`, `!kritik` style priorities, reminders and waiting phrases. Planning time (`doAt`), start, reminder, follow-up and hard deadline remain separate. A date alone is never a hard deadline.
