# Re-entry Workflow

Re-entry uses the existing inactivity signal and presents at most three
actionable tasks, due waiting follow-ups, stale items, and capacity context.
Opening the flow never mutates tasks; completion requires an explicit user
action or exit.
